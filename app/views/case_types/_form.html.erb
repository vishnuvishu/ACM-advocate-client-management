<%= form_for(@case_type) do |f| %>
  <% if @case_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@case_type.errors.count, "error") %> prohibited this case_type from being saved:</h2>

      <ul>
      <% @case_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div><br>

  <div class="actions">
    <%= f.submit "Create Case Type", class: "btn btn-primary" do  %>
      <i class="glyphicon glyphicon-plus"> </i> %> <% end %>
    </div>
<% end %>

