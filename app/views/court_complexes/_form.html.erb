<%= form_for(@court_complex) do |f| %>
  <% if @court_complex.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@court_complex.errors.count, "error") %> prohibited this court_complex from being saved:</h2>

      <ul>
      <% @court_complex.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :state_id %><br>
    <%= f.collection_select :state_id, State.all, :id, :name %>
  </div>
  <div class="field">
    <%= f.label :district_id %><br>
    <%= f.collection_select :district_id, District.all, :id, :name %>
  </div></br>

   <div class="actions">
    <%= f.submit "Create Court Complex", class: "btn btn-primary" do  %>
      <i class="glyphicon glyphicon-plus"> </i> %> <% end %>
    </div>
<% end %>
