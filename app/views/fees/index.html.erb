<div class="container-fluid">


 <div class="main-heading">
    <h1>All Fees</h1>
  </div>

<div class="main-heading-body"> 
<table class="table">
  <thead>
    <tr>
      <th>ID</th>
      <th>Hearing</th>
      <th>Amount paid</th>
      <th>comment</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @fees.each do |fee| %>
      <tr>
        <td><%= fee.id %></td>
        <td><%= fee.try(:hearing_id) %></td>
        <td><%= fee.amount %></td>
        <td><%= fee.comment %></td>
      <!--   <td><%#= link_to hearing_fee_path(fee.hearing, fee), class: "btn btn-success" do %> Show <%# end %></td>
        <td><%#= link_to edit_hearing_fee_path(fee.hearing, fee), class: "btn btn-info" do %>Edit <%# end%></td>
        <td><%#= link_to fee, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger" do %>Delete<%# end %></td>
      </tr> -->
    <% end %>
  </tbody>
</table>

<br>

<%#= link_to 'New Fee', new_fee_path %>
</div>
</div>